// 4 정수 최댓값

#include <iostream>
#include <string>
using namespace std;


int max4(int a, int b, int c, int d)
{
	int max = a;

	if (b > max)
	{
		max = b;
	}
	if (c > max)
	{
		max = c;
	}
	if (d > max)
	{
		max = d;
	}

	return max;
}

int main()
{
	int a, b, c, d;
	cin >> a >> b >> c >> d;

	int maxNum = max4(a, b, c, d);
	cout << "최댓값 : " << maxNum << '\n';

	return 0;
}

// 4 정수 최솟값
#include <iostream>
#include <string>
using namespace std;


int min4(int a, int b, int c, int d)
{
	int min = a;

	if (b < min)
	{
		min = b;
	}
	if (c < min)
	{
		min = c;
	}
	if (d < min)
	{
		min = d;
	}

	return min;
}

int main()
{
	int a, b, c, d;
	cin >> a >> b >> c >> d;

	int minNum = min4(a, b, c, d);
	cout << "최솟값 : " << minNum << '\n';

	return 0;
}

//3 정수 최솟값
#include <iostream>
#include <string>
using namespace std;


int min4(int a, int b, int c)
{
	int min = a;

	if (b < min)
	{
		min = b;
	}
	if (c < min)
	{
		min = c;
	}

	return min;
}

int main()
{
	int a, b, c;
	cin >> a >> b >> c;

	int minNum = min4(a, b, c);
	cout << "최솟값 : " << minNum << '\n';

	return 0;
}
