#include <iostream>
#include <string>
using namespace std;

int main() {
    
    int num = 0;
    cin >> num;
    
    for(int i= 0; i < num ; i++){
        string str = "";
        cin >> str;
        if(str.size() >= 6 && str.size() <= 9){
            cout << "yes\n";
        }
        else{
            cout <<  "no\n";
        }
    }
    
    return 0;
}
